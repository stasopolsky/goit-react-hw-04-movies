import React from "react";
import Loader from "react-loader-spinner";

const Spinner = () => (
  <Loader type="Puff" color="#3d004d" height={100} width={100} timeout={3000} />
);

export default Spinner;
